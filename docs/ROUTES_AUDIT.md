# Аудит маршрутов и ролей

Дата актуализации: 2026-09-11

## Текущее состояние

В проекте есть встроенный Dashboard, служебные маршруты, API авторизации, проекты, monitors, история и heartbeat. Реализована роль владельца self-hosted-установки; административной и командных ролей пока нет.

Все маршруты приложения регистрируются в `internal/httpserver`; других источников HTTP-обработчиков в репозитории нет.

Все перечисленные пути дополняются значением `BASE_PATH`, если приложение установлено под URL-префиксом. Например, `/health` становится `/uptimec/health`.

## Реализованные маршруты

| Метод | Путь | Назначение | Доступ | Проверки |
| --- | --- | --- | --- | --- |
| GET | `/health` | Проверка работы процесса | Публичный | Другие методы получают `405`; ответ не содержит внутренних данных |
| GET | `/ready` | Проверка готовности приложения и PostgreSQL | Публичный | Таймаут проверки БД — 2 секунды; ошибка скрыта; другие методы получают `405` |
| POST | `/api/v1/auth/register` | Регистрация и создание сессии | Публичный | JSON до 16 КиБ, пароль 12–128 байт, Origin, 5 запросов/мин на IP |
| POST | `/api/v1/auth/login` | Вход и создание сессии | Публичный | Одинаковая ошибка для email/пароля, Origin, 10 запросов/мин на IP |
| POST | `/api/v1/auth/logout` | Отзыв текущей сессии | Пользователь | Cookie-сессия и Origin; cookie удаляется |
| GET | `/api/v1/auth/me` | Текущий пользователь | Пользователь | Только действующая неотозванная cookie-сессия |
| GET | `/` | Встроенный Dashboard | Публичный экран входа | CSP, no-store; данные доступны только после auth API |
| GET/POST | `/api/v1/projects` | Пагинированный список и создание проектов | Владелец | Cookie, курсор привязан к владельцу, Origin для POST |
| GET/PATCH/DELETE | `/api/v1/projects/{projectID}` | Проект | Владелец | `project.id` и `user_id`; подтверждение удаления |
| POST | `/api/v1/projects/{projectID}/monitors` | Добавление monitor | Владелец | Владелец проекта, Origin, валидация типа/цели |
| PATCH/DELETE | `/api/v1/projects/{projectID}/monitors/{monitorID}` | Управление monitor | Владелец | Проект, monitor и владелец проверяются на backend |
| GET | `/api/v1/projects/{projectID}/monitors/{monitorID}/checks` | История проверок за период | Владелец | Владелец и monitor проверяются до чтения; курсор привязан к периоду |
| GET | `/api/v1/projects/{projectID}/monitors/{monitorID}/incidents` | История инцидентов за период | Владелец | Владелец и monitor проверяются до чтения; курсор привязан к периоду |
| GET | `/api/v1/projects/{projectID}/monitors/{monitorID}/summary` | Uptime, coverage, response time и последний инцидент | Владелец | SQL одновременно фильтрует project, monitor и владельца; период до 31 дня |
| POST | `/api/v1/heartbeat/{token}` | Сигнал бота/worker | Секретный токен | В БД только SHA-256; неизвестный токен тоже получает `204` |
| GET/POST | `/api/v1/projects/{projectID}/webhooks` | Список и создание webhooks | Владелец | Владелец проекта, Origin для POST, URL по SSRF-политике, до 5 на проект |
| PATCH/DELETE | `/api/v1/projects/{projectID}/webhooks/{webhookID}` | Подписка, включение и удаление webhook | Владелец | Проект, webhook и владелец; Origin; подтверждение удаления |
| GET | `/api/v1/projects/{projectID}/notifications` | Пагинированный журнал доставки | Владелец | Проект сначала проверяется по `id` + `user_id`; курсор привязан к владельцу и проекту |
| POST | `/api/v1/projects/{projectID}/notifications/test` | Пробная асинхронная доставка | Владелец | Cookie, Origin, project owner; создаёт событие без инцидента |
| POST | `/api/v1/projects/{projectID}/notifications/{notificationID}/retry` | Повтор окончательно неудачной доставки | Владелец | Cookie, Origin, project/event owner, только `failed`, `X-Confirm-Retry: true` |

## Реализованные роли

- Неавторизованный посетитель.
- Авторизованный пользователь.

## Предварительная модель доступа

Пользовательская часть модели реализована. Администраторская часть остаётся предложением.

### Неавторизованный посетитель

- регистрация;
- вход;
- health endpoint без внутренних диагностических данных.

### Пользователь

- просмотр и изменение только собственных сайтов;
- просмотр собственных проверок и инцидентов;
- управление собственными настройками уведомлений;
- просмотр собственной аналитики и метрик после появления этих функций.

### Администратор системы

- системная диагностика;
- управление служебными настройками;
- действия поддержки только через явно защищённые маршруты с аудитом.

Администраторская роль не должна автоматически использоваться для обычных пользовательских операций.

## Устаревшие маршруты

Исходники `/api/v1/sites` временно сохранены для совместимости тестов, но основной `main.go` их не регистрирует. Новые клиенты должны использовать `/api/v1/projects`.

## Обязательные проверки для каждого защищённого маршрута

- Авторизация проверяется на backend.
- Доступ проверяется по владельцу ресурса, а не только по наличию входа.
- Идентификаторы из URL не считаются доказательством доступа.
- Входные данные валидируются и имеют ограничения размера.
- Ошибки не раскрывают секреты, SQL и внутреннюю структуру приложения.
- Изменяющие и административные действия журналируются без токенов и паролей.
- Для публичных и дорогих операций применяется ограничение частоты запросов.

## Риски, которые нужно проверить при реализации

- IDOR: доступ к чужому сайту через замену `siteID`.
- SSRF: запросы мониторинга к локальным и служебным сетевым адресам.
- Brute force на маршруте входа.
- Массовое создание сайтов и заданий проверки.
- Утечка диагностической информации через health endpoint и ошибки.
- Небезопасное удаление сайтов и связанных данных.

## Правило обновления документа

После добавления или изменения маршрута нужно зафиксировать здесь метод, путь, назначение, требуемую роль и проверку владельца. Когда появится реальное API, подробные форматы запросов и ответов следует вынести в `docs/API.md`.
