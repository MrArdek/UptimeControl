# План проекта

Дата актуализации: 2026-09-06

## Главная цель проекта

Создать self-hosted-сервис, который пользователь устанавливает на свой сервер и открывает на отдельном домене или по пути вроде `https://igra.ru/uptimec`. Сервис объединяет мониторинг проектов, веб-аналитику и метрики серверов в одном интерфейсе.

План охватывает полный продукт из [README](../README.md), включая региональные проверки, посещаемость, поведение посетителей, производительность, Custom Events, Custom Properties и Server Agent. MVP — завершённый фундамент, после которого разработка продолжается по этапам 8–19. Необязательные направления из README вынесены в этап 20, чтобы они тоже имели проверяемый план.

Статусы: **done** — уже реализовано по документации и существующему коду; **planned** — предстоит реализовать; **conditional** — отдельное расширение после решения о его необходимости. Описание будущего этапа не означает, что функция уже доступна. Состояние реализации хранится в [PROJECT_STATE.md](PROJECT_STATE.md), результаты выполненных проверок — в [VERIFICATION.md](VERIFICATION.md).

Указанные ниже критерии готовности — будущая приёмка, а не отчёт о проведённых тестах. Сроки и оценки трудозатрат назначаются при декомпозиции этапа после выбора технических решений.

## Завершённый MVP: исходная точка

Первая рабочая версия позволяет владельцу установленного экземпляра создать проект, добавить к нему HTTP/HTTPS- или heartbeat-проверку и увидеть текущий статус, время ответа и историю инцидентов.

Проектом может быть сайт, API, игра, Telegram-бот, cron или другой сервис. HTTP monitor проверяет публичный URL, а heartbeat monitor контролирует сервис без frontend по входящему сигналу. TCP и Server Agent остаются следующими этапами.

Веб-аналитику, Server Agent и сложные региональные проверки лучше добавлять после устойчивой работы базового мониторинга.

## Этапы 1–7: реализованный фундамент

### 1. Уточнить требования MVP

Описание:

- определить обязательные функции первой версии;
- выбрать способ авторизации;
- определить роли пользователей;
- согласовать минимальные параметры проверки сайта и уведомлений.

Зачем:

Это позволит не смешивать в первой реализации мониторинг, аналитику и серверные метрики.

Риски:

- без зафиксированных границ MVP проект может стать слишком большим до появления первой рабочей версии.

Как проверить:

- список функций MVP согласован и отражён в документации.

Статус: **done**

Принято:

- self-hosted как основной способ поставки;
- работа на отдельном домене или под URL-префиксом через `BASE_PATH`;
- универсальная модель «проект + monitors» с переносом старых данных;
- PostgreSQL как основная база;
- email и пароль для первого способа входа;
- серверная сессия в защищённой cookie;
- первый пользователь становится единственным владельцем установки в MVP;
- Telegram как первый канал уведомлений.

Результат:

- границы self-hosted MVP зафиксированы;
- повторная регистрация после bootstrap владельца закрывается;
- новые зависимости требуют предварительного GitHub issue.

### 2. Подготовить каркас Go-приложения

Описание:

- выбрать HTTP-роутер и структуру пакетов;
- добавить конфигурацию приложения;
- реализовать корректное завершение сервера;
- добавить endpoint проверки состояния приложения.

Возможные области:

- `main.go`;
- `cmd/`;
- `internal/config/`;
- `internal/http/`.

Риски:

- преждевременное усложнение структуры;
- выбор тяжёлых зависимостей без необходимости.

Как проверить:

- приложение запускается;
- health endpoint возвращает успешный ответ;
- тесты проходят.

Статус: **done**

Результат:

- используется HTTP-сервер из стандартной библиотеки Go;
- добавлены таймауты сервера и корректное завершение;
- реализован `GET /health`;
- добавлены тесты конфигурации и маршрута;
- сборка и тесты проверены на Go 1.27.1.

### 3. Подключить PostgreSQL и миграции

Описание:

- добавить безопасное чтение строки подключения из окружения;
- выбрать инструмент миграций;
- создать начальные таблицы пользователей, сайтов и проверок;
- добавить проверку соединения с базой.

Риски:

- не хранить пароль базы в коде;
- не изменять схему вручную в production;
- заранее продумать рост таблицы результатов проверок.

Как проверить:

- чистая база разворачивается миграциями;
- приложение подключается к ней;
- повторный запуск миграций безопасен.

Статус: **done**

Результат:

- добавлен пул соединений `pgx/v5`;
- `DATABASE_URL` проверяется до запуска;
- миграции применяются транзакционно и защищены advisory lock;
- созданы таблицы `users`, `sessions`, `sites`, `uptime_checks` и `incidents`;
- `GET /ready` проверяет доступность базы;
- чистая база, повторный запуск миграций и запуск сервера проверены на PostgreSQL 18.6.

### 4. Реализовать пользователей и авторизацию

Описание:

- регистрация и вход;
- безопасное хеширование паролей;
- проверка авторизации на backend;
- разделение данных разных пользователей.

Риски:

- способ авторизации должен быть согласован до реализации;
- токены, cookie и пароли нельзя логировать.

Как проверить:

- пользователь видит только собственные сайты;
- неверные учётные данные не дают доступ;
- защищённые маршруты недоступны без авторизации.

Статус: **done**

Результат:

- реализованы регистрация, вход, выход и `/me`;
- пароль хешируется Argon2id;
- cookie имеет `HttpOnly`, `SameSite=Lax` и `Secure` по умолчанию;
- в БД хранится только SHA-256-хеш случайного токена сессии;
- добавлены одинаковая ошибка неверных данных входа, Origin-проверка и rate limit;
- модульные и сквозные проверки с PostgreSQL пройдены.

### 5. Реализовать базовый мониторинг проектов

Описание:

- добавление и изменение сайта;
- периодические HTTP-проверки;
- сохранение статуса и времени ответа;
- создание и закрытие инцидентов;
- история результатов.

Риски:

- защита от SSRF при запросах к пользовательским URL;
- ограничения времени ответа, размера тела и количества перенаправлений;
- контроль конкурентности и частоты проверок.

Как проверить:

- доступный тестовый сайт получает статус online;
- недоступный сайт получает статус offline;
- падение создаёт один инцидент, а восстановление закрывает его.

Статус: **done**

Результат:

- реализованы `projects` и несколько `monitors` внутри проекта;
- старые sites и их история переносятся миграцией без потери данных;
- HTTP checker повторно проверяет DNS/IP перед каждым соединением и redirect;
- heartbeat контролирует Telegram-боты, cron и workers без frontend;
- планировщик безопасно забирает задания через PostgreSQL `SKIP LOCKED`;
- результаты записываются в `uptime_checks`;
- падение открывает один инцидент, восстановление закрывает его;
- добавлены API истории проверок и инцидентов;
- HTTP- и heartbeat-сценарии проверены на PostgreSQL 18.6.

### 6. Добавить уведомления и простой Dashboard

Описание:

- показать список сайтов, состояние и историю инцидентов;
- добавить первый канал уведомлений;
- отображать понятные ошибки настройки.

Риски:

- не раскрывать данные одного пользователя другому;
- ограничивать повторные уведомления.

Как проверить:

- пользователь видит актуальное состояние своих сайтов;
- при падении и восстановлении отправляются ожидаемые уведомления.

Статус: **done**

Результат:

- встроенный адаптивный Dashboard не требует отдельного Node.js frontend;
- Dashboard работает с `BASE_PATH`, показывает проекты, monitors, состояния и историю;
- Telegram-уведомления включаются двумя переменными окружения;
- уведомления отправляются только при открытии и закрытии инцидента;
- исходящие webhooks: до 5 на проект, события `down`/`recovered`, HMAC-подпись, retry до 3 попыток, журнал `webhook_deliveries`; Telegram при этом не затронут.

Развитие: асинхронная очередь доставки в этапе 10 и SSE для живого Dashboard в этапе 11.

### 7. Подготовить независимую self-hosted-установку

Описание:

- установить Uptime Control на отдельном сервере от контролируемого проекта;
- использовать отдельный поддомен, напрямую указывающий на monitoring VPS;
- подготовить Docker Compose, PostgreSQL, systemd и reverse proxy.

Статус: **done**

Результат:

- добавлены `Dockerfile`, `compose.yaml` и примеры deployment-конфигурации;
- приложение и PostgreSQL могут работать на соседнем сервере;
- порт PostgreSQL не публикуется, HTTP слушает только loopback;
- задокументировано, почему `uptime.igra.ru` надёжнее, чем проксирование через `igra.ru/uptimec` на основном сервере.

## Правила реализации полного продукта

- Сохранять self-hosted-установку, работу в корне домена и с `BASE_PATH`, модель `projects` + `monitors` и историю существующих установок. `sites` не развивать; пример `data-site-id` из README согласовать с идентификатором источника аналитики внутри проекта.
- Go остаётся языком backend, Regional Check Nodes и Server Agent; PostgreSQL и явный SQL — исходным хранилищем. Новые таблицы и индексы добавлять новыми миграциями, не изменяя применённые.
- TypeScript, React, предпочтительный Next.js и сборка трекера входят в целевую архитектуру README, но ещё не внедрены. До добавления языка, библиотеки, runtime, GeoIP-базы или внешнего сервиса нужны GitHub issue с альтернативами, лицензией, стоимостью сопровождения и рисками, затем явное разрешение пользователя согласно [DECISIONS.md](DECISIONS.md) и [AGENTS.md](../AGENTS.md). Сам план зависимостей не добавляет.
- Для каждого потока определить владельца данных, аутентификацию, лимиты, срок хранения и поведение при перегрузке. Проверки доступа, SSRF-защита и исключение секретов из логов остаются обязательными.
- Каждый этап делить на небольшие изменения. Этап считается завершённым после реализации API, хранения, соответствующего интерфейса, проверок и актуализации документации; наличие заглушки не является готовностью.

## Покрытие требований README

| Раздел README | Результат полного продукта | Этапы |
|---|---|---|
| О проекте и состояние MVP | Self-hosted, универсальные проекты, HTTP/HTTPS, heartbeat, совместимость установки | 1–7, 19 |
| 1. Мониторинг доступности | Статус, HTTP code, response time, история, начало/длительность/окончание падения; региональные результаты | 5, 9, 12 |
| 2. Web Analytics | Онлайн, уникальные пользователи за день/неделю/месяц, сессии, pageviews, запросы, нагрузка и время загрузки | 8, 13, 14, 16 |
| 3. Данные о посетителях | Источники, все описанные UTM, referrer, landing page, устройство, страна, браузер, страницы и клики | 13–15 |
| 4. Производительность | Время ответа и загрузки, число запросов, динамика и пики на графиках | 9, 16, 18 |
| 5. Мониторинг сервера | CPU, RAM, Disk, Load Average, процессы, входящий/исходящий трафик и история | 17, 18 |
| 6. Custom Events | `uptime.track`, параметры, проект, время, сессия, страница, UTM и источник | 15 |
| 7. Custom Properties | `uptime.identify`, свойства пользователя/сессии, фильтрация и просмотр | 15 |
| 8. Компоненты сбора | Go Check Nodes, браузерный Tracker, Go Server Agent с раздельными полномочиями | 12, 13, 17 |
| 9. Общая схема работы | Три потока сходятся в backend и связываются с одним проектом | 8, 12–18 |
| 10. Dashboard | Статус и uptime, посещаемость, производительность, источники, поведение и Server | 11, 18 |
| 11. Архитектура сбора | Monitoring API/Storage, Analytics API/Storage, Metrics API/Storage | 8, 12, 13, 17 |
| 12. Стек | Go + TypeScript/React, выбор Next.js, SQL/PostgreSQL, очереди и заменяемое хранилище временных рядов | 8, 10, 11, 13, 19 |
| 12. Python и расширенная аналитика | Аномалии, прогнозы, поведенческий анализ, ML и расширенная статистика при обоснованной необходимости | 20 |

## Этапы 8–19: полный продукт

### 8. Зафиксировать контракты данных, метрик и хранения

Статус: **planned**. Зависит от: 1–7. Открывает: все новые потоки сбора данных.

Что сделать:

- Описать модель связей: проект → monitors, источники аналитики, региональные узлы, серверы и агенты. Для каждого потока определить версию протокола, время события и приёма, UTC-хранение, часовой пояс отчётов, единицы измерения, допустимое опоздание и дедупликацию.
- Зафиксировать определения online, уникального посетителя, сессии, pageview, uptime, среднего и пикового значения; определить окна и правила для пустых периодов, пропусков наблюдений, пауз мониторинга и смены часового пояса.
- Разделить три показателя: синтетическое время ответа monitor, браузерное время загрузки и серверное время обработки запроса. Разделить браузерные resource requests, запросы приложения и собственные запросы Uptime Control: их нельзя суммировать как посещаемость.
- Спроектировать Monitoring, Analytics и Metrics API с версионированием, batch-приёмом, пагинацией, ограничениями размера/частоты/кардинальности и понятными ошибками. Для повторной отправки задать устойчивые идентификаторы событий или образцов.
- Определить таблицы сырых событий и агрегатов, индексы по проекту и времени, минутные/часовые/дневные сводки, сроки хранения и фоновую очистку. Уникальных за неделю/месяц считать по идентификаторам периода, а не суммой дневных уникальных; средние объединять с весами, пики сохранять до укрупнения интервалов.
- Выделить интерфейсы хранилищ, чтобы позднее перенести временные ряды без изменения бизнес-логики. Начать с PostgreSQL; секционирование и внешнее хранилище вводить по измеренной нагрузке.
- Согласовать бюджет нагрузки: число проектов/monitors/регионов/агентов, событий в секунду, срок хранения, размер диска и целевые задержки API/сбора. Эти значения станут проверяемыми условиями этапа 19.

Критерии готовности:

- В `API.md`, `DATABASE.md` и `DECISIONS.md` описаны контракты, формулы, единицы, границы доверия и измеримые бюджеты.
- На контрольном наборе данных определены ожидаемые итоги для повторов, запоздалых событий, перехода суток и отсутствующих измерений; согласован порядок миграций без потери текущей истории.

### 9. Довести базовый мониторинг до полного набора показателей

Статус: **planned**. Зависит от: 8.

Что сделать:

- Дополнить историю и сводки фильтрами по проекту, monitor и периоду, пагинацией, uptime за выбранный период, последним инцидентом и длительностью текущего/закрытого падения.
- Добавить графики response time, средние и пиковые значения с явным обозначением пробелов в наблюдениях.
- Добавить TCP-monitor для сервисов без HTTP: адрес/порт, интервал, таймаут, время соединения и ошибка; использовать те же правила инцидентов и проверки допустимости адресов.
- Определить переходы `unknown`/online/offline, поведение отключённого monitor, порог подтверждения падения/восстановления и обработку временного отсутствия проверок.
- Сохранить HTTP status code только там, где он существует; TCP и heartbeat не должны получать выдуманный HTTP-статус.

Критерии готовности:

- Контрольная последовательность успешных/неуспешных проверок даёт ожидаемый uptime, начало и конец инцидента и не создаёт дубликатов.
- HTTP, HTTPS, heartbeat и TCP проходят проверки состояния, таймаутов и доступа; пагинация возвращает все записи, в том числе сверх текущего ограничения 100 проектов.
- История сохраняется после обновления с MVP; отображение включено в этапы 11 и 18.

### 10. Сделать доставку уведомлений устойчивой

Статус: **planned**. Зависит от: 8, 9.

Что сделать:

- Вынести Telegram и webhooks из синхронного пути планировщика в постоянную очередь PostgreSQL. Записывать событие доставки транзакционно с изменением инцидента.
- Реализовать захват задания, ограничение конкурентности, таймаут, повторные попытки с задержкой, восстановление после рестарта и журнал окончательно неудачных доставок.
- Задать стабильный ID события, семантику доставки «как минимум один раз» и правила дедупликации для получателя: сбой после отправки, но до фиксации результата не позволяет обещать доставку ровно один раз.
- Добавить настройки каналов и подписок проекта, пробную отправку, понятные ошибки, просмотр попыток и безопасный повтор. Сохранить HMAC-подпись webhook и существующие Telegram-настройки.
- Предусмотреть расширяемый интерфейс каналов; email и другие интеграции добавлять по отдельному решению после выбора транспорта.

Критерии готовности:

- Медленный получатель не задерживает проверки; рестарт не теряет очередь; два воркера не выполняют одно активное задание одновременно.
- Подтверждены подпись, retry, лимиты и отсутствие секретов в истории. Реальная доставка Telegram и webhook проверена на настроенных владельцем тестовых получателях.

### 11. Подготовить полноценный frontend и живой Dashboard

Статус: **planned**. Зависит от: 8–10 и согласования frontend-зависимостей.

Что сделать:

- Зафиксировать выбор TypeScript + React и оценить предпочтительный Next.js: статическая сборка с раздачей из Go либо отдельный frontend runtime. Сравнить поддержку `BASE_PATH`, сложность установки, размер и необходимость SSR; оформить решение до внедрения.
- Реализовать вход, проекты, monitors, историю, инциденты, настройки и уведомления с сохранением существующих API и сессий.
- Создать общие компоненты графиков, таблиц, фильтров периода и часового пояса, пагинации, состояний загрузки/ошибки/пустых данных. Обеспечить мобильную версию, клавиатурное управление и доступные подписи.
- Добавить SSE для состояния monitors и инцидентов: авторизация, переподключение, восстановление актуального снимка после разрыва, ограничение соединений и polling как резервный режим.
- Подготовить разделы аналитики, событий, свойств и серверов; подключать их к реальным данным по мере завершения следующих этапов.

Критерии готовности:

- Основные сценарии MVP проходят в новом интерфейсе на отдельном домене и под `/uptimec`; обновление страницы по вложенному маршруту работает.
- Logout прекращает доступ к данным и потоку, разрыв SSE не оставляет устаревший статус без предупреждения, интерфейс корректно работает на узком экране и с клавиатуры.
- Сборка и поставка frontend воспроизводимы; требования к build/runtime явно отражены в инструкции установки.

### 12. Реализовать Regional Check Nodes

Статус: **planned**. Зависит от: 8–11.

Что сделать:

- Создать отдельный Go-бинарник регионального узла с конфигурацией региона и адреса backend, регистрацией, индивидуальными отзываемыми учётными данными и health/heartbeat узла.
- Организовать получение назначенных HTTP/HTTPS/TCP-заданий и передачу результатов через Monitoring API; узлы не получают прямого доступа к БД. Входящий heartbeat сервиса остаётся отдельным типом сигнала.
- Хранить node ID, регион, дату/время, доступность, HTTP code при наличии, response time и ошибку. Ограничить область действий узла назначенными ему проверками.
- Реализовать срок аренды задания, дедупликацию результатов, ограниченный локальный буфер при потере связи, обработку запоздания и рассинхронизации часов.
- Сохранить SSRF-политику и ограничения запросов на каждом узле. Недоступность узла показывать как отсутствие наблюдений, отдельно от недоступности сайта.
- Позволить владельцу выбирать регионы, видеть состояние каждого и общий статус. Определить правило региональной деградации и общего падения, кворум/порог и уведомления без дублирования одного глобального инцидента.
- Подготовить установку узлов в Европе, Азии и Северной Америке и добавление произвольного региона без изменения кода; инфраструктуру выбирает и предоставляет владелец.

Критерии готовности:

- В тестовой среде несколько узлов проверяют один monitor; изолированная региональная ошибка отличается от общего падения, выключение узла даёт пропуск наблюдений.
- Повторный результат не удваивает историю; отозванный узел не может отправлять данные; Dashboard показывает все поля результата по регионам.
- Отдельно проведена проверка на действительно географически разнесённых серверах: локальная эмуляция трёх регионов её не заменяет.

### 13. Реализовать Analytics API и JavaScript Tracker

Статус: **planned**. Зависит от: 8, 11 и согласования TypeScript/tooling.

Что сделать:

- Добавить источник аналитики внутри проекта с публичным ключом сбора, разрешёнными origin и инструкцией подключения. Публичный ключ трекера не даёт доступ к чтению аналитики или управлению проектом.
- Поставлять трекер с self-hosted-экземпляра с учётом `BASE_PATH`; адрес CDN из README остаётся примером, внешний CDN не должен быть обязательным.
- Написать трекер на TypeScript и выпускать версионированный минимизированный JavaScript: асинхронная загрузка, отсутствие блокировки страницы, изоляция ошибок, batch-отправка и ограниченная очередь/retry.
- Собирать pageviews, посещённую страницу, сессию, псевдонимный ID посетителя, landing page, referrer, `utm_source`, `utm_campaign`, `utm_medium`, устройство и браузер. Поддержать обычную навигацию и SPA-переходы без двойного pageview.
- Ввести heartbeat активности и обработку скрытой/закрытой вкладки для online; определить поведение нескольких вкладок, запрета хранилища браузера и смены пользователя.
- Для страны выбрать серверное GeoIP-обогащение или доверенный источник proxy-метаданных; согласовать лицензию и обновление GeoIP-данных. При отсутствии данных показывать «неизвестно», не доверять произвольному заголовку клиента.
- Реализовать валидацию схемы, лимиты, проверку разрешённых origin и защиту от массовой подмены событий; учитывать, что публичный endpoint нельзя сделать достоверным только CORS-проверкой.
- Добавить настройки приватности: включение/отключение сбора, согласие при выбранном владельцем режиме, исключение чувствительных URL/query и полей, срок хранения и удаление данных. Не собирать содержимое форм, пароли и произвольный текст страницы.

Критерии готовности:

- На тестовом сайте и SPA фиксируются ожидаемые pageviews, сессии, UTM и landing page; сбой коллектора не ломает сайт и не вызывает бесконечный рост очереди.
- При выключенном сборе данные не отправляются; повторная отправка не удваивает события; чужой проект не может читать результаты по публичному ключу.
- Проверены размер gzip-сборки и накладные расходы относительно бюджета этапа 8; инструкция установки работает без внешнего CDN.

### 14. Реализовать отчёты посещаемости и источников

Статус: **planned**. Зависит от: 8, 13.

Что сделать:

- Показать пользователей онлайн, уникальных за день/неделю/месяц и произвольный период, сессии и просмотры страниц; явно указать окно online и ограничения определения уникальности между устройствами.
- Добавить графики активности по времени, пиковые значения и сравнение периодов, фильтры по проекту, странице и источнику.
- Реализовать отчёты по источнику перехода, `utm_source`, `utm_campaign`, `utm_medium`, referrer, landing pages, странам, устройствам и браузерам.
- Сохранить отдельные категории прямого/неизвестного трафика, нормализацию URL и источников, правила атрибуции сессии. Задать обработку ботов и внутреннего трафика.
- Запускать пересчитываемые агрегаты идемпотентно; учитывать запоздалые события и границы retention без изменения уже вычисленного смысла отчёта.

Критерии готовности:

- Данные набора с повторными посетителями, несколькими вкладками и переходом суток дают заранее рассчитанные online, unique, session и pageview; недельные уникальные не равны механической сумме дневных.
- Фильтры и таблицы источников согласуются с общими итогами; время ответа отчётов укладывается в бюджет на согласованном объёме данных.

### 15. Добавить поведение, Custom Events и Custom Properties

Статус: **planned**. Зависит от: 13, 14.

Что сделать:

- Реализовать `uptime.track(name)` и `uptime.track(name, properties)`, в том числе `signup`, `purchase`, `button_click`, `form_submit`, `video_play` из README; вызовы до загрузки трекера помещать в ограниченную очередь.
- Для события сохранять проект/источник, имя, дату/время, ID сессии, страницу, параметры разработчика, связанные UTM и источник перехода.
- Реализовать `uptime.identify(properties)` для свойств пользователя или сессии: `user_id`, `tariff`, `source`, тип аккаунта, категория клиента, источник регистрации и вариант A/B-теста. Зафиксировать область действия, обновление, сброс при logout и влияние изменения свойств на исторические события.
- Валидировать типы, имена, длины, размер, вложенность и число свойств; ограничивать кардинальность. Переданный `user_id` — идентификатор внутри проекта, он не является учётной записью Uptime Control и не связывает разные проекты.
- По отдельному включению расширенной аналитики собирать количество кликов, безопасные идентификаторы элементов/ссылок и последовательность страниц/событий в сессии. Расширенный сбор по умолчанию выключен.
- Добавить просмотр последовательности действий, отчёты по событиям, фильтры по свойствам и страницам, отображение параметров с защитой от внедрения HTML. Автоматически исключать секретные/чувствительные поля согласно настройкам сбора.

Критерии готовности:

- Все примеры `track` и `identify` из README работают, параметры и атрибуция доступны в интерфейсе; невалидные события отклоняются понятной ошибкой.
- Смена пользователя не переносит свойства предыдущего, повтор пакета не удваивает событие, свойства одного проекта не видны другому.
- При выключенной расширенной аналитике автоматические клики и детальный путь не собираются; разрешённые custom events продолжают работать независимо от автосбора кликов.

### 16. Реализовать метрики производительности сайта и запросов

Статус: **planned**. Зависит от: 8, 9, 13, 14.

Что сделать:

- Добавить в трекер измерения времени загрузки и браузерного ожидания ответа через доступные Navigation/Resource Timing API; указать ограничения cross-origin, кеша и SPA. Отсутствие измерения не записывать как нулевую длительность.
- Сохранять образцы и агрегаты во времени, среднее время загрузки сайта, среднее синтетическое время ответа, количество наблюдаемых браузерных запросов, запросы по времени, динамику и пики.
- Для полного числа запросов к сайту/API, включая клиентов без JavaScript, спроектировать отдельный приём агрегированных серверных счётчиков: интеграция приложения или опциональный модуль агента для выбранного access-log/счётчика. Определить поддержанный формат, настройку источника и его проверку.
- Хранить происхождение показателя, единицы и покрытие сбора; не подменять серверный RPS числом pageviews, сетевыми байтами или синтетическими проверками. Исключать двойной учёт одного источника.
- Добавить графики со сменой периода и детализации; связать всплески времени ответа и нагрузки с инцидентами, посещаемостью и серверными показателями.

Критерии готовности:

- Тестовая страница с известными задержками и числом ресурсов даёт ожидаемые измерения в пределах описанной погрешности.
- Контрольная нагрузка на сервер, в том числе без трекера, отражается в серверном счётчике; при неподключённом источнике интерфейс показывает отсутствие серверных данных.
- Укрупнение интервала сохраняет взвешенные средние и пики, пропуски видны на графиках; browser time и monitor response time подписаны раздельно.

### 17. Реализовать Go Server Agent и Metrics API

Статус: **planned**. Зависит от: 8, 11.

Что сделать:

- Создать самостоятельный Go-бинарник агента; первой поддерживаемой платформой предложить Linux VPS, точные ОС/архитектуры и ограничения контейнерного режима зафиксировать до реализации. Поставлять без дополнительного runtime.
- Добавить регистрацию сервера в проекте, индивидуальные credentials с ротацией/отзывом, интервалы сбора и исходящее TLS-соединение с Metrics API без открытого управляющего порта агента.
- CPU: текущая, средняя, пиковая загрузка и история; определить расчёт по интервалу и нормализацию по числу ядер.
- RAM: общий, используемый, свободный объём и процент; документировать учёт cache/buffers и доступной памяти.
- Disk: общий объём, занятое/свободное место и процент заполнения по файловым системам, с фильтрацией псевдофайловых систем.
- Load Average: сохранять 1/5/15-минутные показатели во времени и количество CPU для интерпретации.
- Процессы: PID, безопасное имя, CPU/RAM, сортировка и ограниченный top-N; сбор включается настройкой и работает с минимальными правами. Не передавать environment и командные аргументы с возможными секретами.
- Сеть: входящие/исходящие байты, скорость, динамика и пики по интерфейсам и суммарно; корректно обрабатывать сброс счётчика, рестарт и исчезновение интерфейса.
- Реализовать пакетную отправку, ограниченный буфер при недоступности backend, retry, идентификаторы образцов и heartbeat агента. Отображать устаревание данных; агент не исполняет удалённые команды.
- Документировать установку через systemd, обновление, удаление, требования к правам и диагностику; добавить интерфейс управления серверами и агентами.

Критерии готовности:

- CPU/RAM/Disk/Load Average/процессы/сеть сверены с системными показателями на поддержанной платформе в простое и под контролируемой нагрузкой.
- После рестарта счётчики не дают отрицательного трафика или ложного пика; потеря связи не вызывает неограниченного роста памяти/диска.
- Отзыв credentials блокирует приём; агент не пишет в другой проект; неизвестные или устаревшие данные не отображаются как здоровый сервер.

### 18. Объединить все данные в Dashboard проекта

Статус: **planned**. Зависит от: 9–17.

Что сделать:

- Собрать обзор проекта с разделами из README: статус, посещаемость, производительность, источники, поведение и Server; применять общий период и часовой пояс.
- Статус: Online/Offline, uptime, последний инцидент, response time и результаты по регионам с обозначением неполных наблюдений.
- Посещаемость: online, пользователи за день/неделю/месяц, pageviews и sessions; производительность: средние времена ответа/загрузки, число запросов и пики нагрузки.
- Источники: source/UTM/referrer/landing pages/страны/устройства/браузеры; поведение: страницы, клики, custom events и custom properties.
- Server: текущие и исторические CPU/RAM/Disk/Load Average, процессы, входящий/исходящий трафик и пики; переход от проекта к конкретному серверу.
- Сделать связанные переходы от инцидента к временному интервалу графиков, региону, посещаемости и серверным метрикам без утверждения автоматической причинности.
- Добавить инструкции подключения трекера/узла/агента и различимые состояния «не подключено», «нет данных», «устарело», «ошибка». Управление каналами, событиями, свойствами и агентами включить в соответствующие экраны.

Критерии готовности:

- Один тестовый проект с подключёнными тремя потоками показывает каждый показатель раздела 10 README; цифры совпадают с API на одном периоде.
- Проект без сайта, трекера или агента остаётся пригодным для мониторинга; отсутствующие интеграции не создают ложные нули и ошибки всего Dashboard.
- Сквозные браузерные сценарии проверяют настройку, фильтры, живое обновление, навигацию, ошибки, права доступа и мобильный экран.

### 19. Подготовить и принять полный self-hosted-релиз

Статус: **planned**. Зависит от: 8–18.

Что сделать:

- Настроить CI для Go-тестов, race, vet и сборки; после согласования frontend-стека добавить его проверки и воспроизводимую сборку. Проверять миграции на чистой БД и обновление с существующего MVP.
- Собрать релизные артефакты backend, узла, агента и frontend/tracker для объявленных платформ; проверить Docker Compose и systemd на Linux, reverse proxy, TLS и `BASE_PATH`.
- Прогнать нагрузку по бюджету этапа 8: проверки × регионы, события аналитики, агенты, отчёты и SSE одновременно. Измерить задержки, ресурсы, рост диска и влияние очистки/агрегации.
- Проверить устойчивость к рестартам, недоступной БД, медленным получателям, потере узла и переполнению очередей. Для нескольких backend-реплик обеспечить координацию заданий и общее ограничение запросов; если режим не поддерживается, явно ограничить релиз одной репликой.
- Реализовать и проверить retention, очистку и пересчёт агрегатов; отделить запуск миграций и DDL-права от runtime там, где этого требует выбранная схема развёртывания.
- Проверить резервное копирование и восстановление БД, конфигурации и необходимых секретов, обновление и безопасный возврат версии приложения с совместимой схемой. Согласовать и измерить допустимую потерю данных и время восстановления.
- Завершить эксплуатацию доступа владельца: безопасное восстановление доступа/смена пароля, отзыв сессий, очистка истёкших сессий, процедуры удаления данных. Email-подтверждение, MFA и командные роли рассматривать отдельными расширениями этапа 20.
- Добавить собственные показатели состояния сервиса: задержка планировщика, очередь доставки/сбора, ошибки обработки, состояние узлов/агентов и объём хранения, с исключением секретов из логов.
- Обновить `SETUP`, `SERVER_INSTALL`, `BACKUP`, `API`, `DATABASE`, `SECURITY`, `ROUTES_AUDIT`, `PROJECT_MAP`, `PROJECT_STATE` и `VERIFICATION`; привести README к фактически выпущенным возможностям, указать ограничения и совместимые версии компонентов.

Критерии готовности полного релиза:

- Все обязательные строки матрицы покрытия имеют реализованные API/хранение/UI и записанный результат приёмки; ни одна основная функция README не осталась только заглушкой или планом.
- Новый владелец по инструкции устанавливает систему, подключает проект, региональный узел, трекер и агент, получает уведомление и видит все шесть разделов Dashboard.
- Установка с MVP обновляется с сохранением владельца, проектов, monitors, проверок и инцидентов; восстановление из резервной копии проверено в отдельной среде.
- Нагрузочные и сквозные проверки укладываются в согласованные бюджеты, ограничения платформ и непроверенные интеграции явно задокументированы. Нельзя отмечать релиз проверенным по одним модульным тестам.

## Этап 20: расширения после полного релиза

### 20.1. Аномалии, прогнозы и расширенная статистика

Статус: **conditional**. Зависит от: 14–19, накопленной истории и согласованных критериев качества.

README описывает Python как возможный отдельный аналитический сервис. Это направление входит в дорожную карту, но не требует Python и ML для выпуска основного продукта.

Что сделать:

- Выбрать конкретные задачи: необычные скачки трафика, аномалии CPU/RAM, прогноз нагрузки и возможных проблем, анализ поведения и расширенная статистика.
- Начать с объяснимых порогов и статистических базовых методов, учесть сезонность, пропуски, минимальную историю и ложные срабатывания.
- Проверить, даёт ли ML достаточный выигрыш; добавлять отдельный Python Analytics Service только при обоснованной задаче, которую невыгодно решать на Go, после issue и согласования зависимостей.
- Определить версионированный обмен агрегатами, изоляцию персональных данных, ограничения ресурсов, поведение при недоступности сервиса и отображение уверенности/объяснения результата.
- Добавить найденные аномалии и прогнозы в Dashboard и опциональные уведомления, сохранив обычный мониторинг независимым от аналитического сервиса.

Критерии готовности:

- На отложенном по времени наборе истории измерены качество обнаружения, число ложных тревог и ошибка прогноза относительно базового метода; пороги приёмки согласованы до внедрения.
- При недостатке истории прогноз не выдаётся как достоверный; отключение сервиса не влияет на проверки, сбор событий и метрик.

### 20.2. Дополнительные продуктовые возможности

Статус: **conditional**. Эти возможности развивают продукт и прежний план, но не являются обязательными функциями README.

- Командная работа: приглашения и роли owner/admin/viewer, проектные разрешения и аудит изменений. Приёмка — матрица доступа и невозможность обхода роли через API.
- Email-подтверждение, восстановление по email и MFA: выбрать провайдера/протокол, предусмотреть истечение/одноразовость токенов и процедуру восстановления. Приёмка — полный сценарий включения, входа, отзыва и восстановления.
- Дополнительные каналы уведомлений: определить востребованные интеграции; применять очередь и правила доставки этапа 10. Приёмка — реальная доставка и корректный повтор/отказ выбранного канала.
- Специализированное хранилище временных рядов и read replicas: вводить, если измерения PostgreSQL не укладываются в бюджет. Приёмка — сверка данных после переноса, измеренный выигрыш и проверенная процедура возврата.

## Порядок работы и ближайшая задача

1. Выполнить этап 8: контракты и определения метрик, схема хранения, бюджеты и решения по новым компонентам.
2. Довести мониторинг и уведомления: 9 → 10, затем основу frontend: 11.
3. Добавить региональные проверки: 12; затем трекер и аналитику: 13 → 14 → 15 → 16.
4. Реализовать Server Agent: 17. Он зависит от 8 и 11 и может быть запланирован раньше аналитики при соответствующем приоритете; это не меняет зависимости общего Dashboard.
5. Объединить интерфейс и пройти приёмку полного релиза: 18 → 19.
6. После полного релиза отдельно оценить расширения 20.1–20.2.

## Внешние условия и открытые решения

- Реальная доставка Telegram требует тестовых `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`; внешнего webhook — согласованного тестового получателя. Секреты не сохранять в документации или Git.
- Реальная межрегиональная приёмка требует серверов в выбранных регионах, удалённая установка — тестового Linux-сервера и домена/TLS. До предоставления инфраструктуры можно реализовать протоколы и локальные интеграционные проверки, но нельзя считать внешнюю приёмку завершённой.
- До внедрения выбрать frontend-способ поставки, GeoIP-источник, поддерживаемые ОС/архитектуры агента и источник полного серверного счётчика запросов. Кандидаты и альтернативы фиксируются на этапах 8, 11, 13, 16 и 17.
- На этапе 8 согласовать определения уникальности/онлайн/сессии, приватность и retention, нагрузочные бюджеты; на этапе 19 — цели восстановления и поддерживаемую топологию развёртывания.
- Бюджет инфраструктуры, согласование зависимостей и реальные интеграционные credentials являются условиями соответствующих задач, а не причиной останавливать всю разработку плана.
