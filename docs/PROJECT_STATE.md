# Состояние проекта

Дата актуализации: 2026-09-08

## Что уже готово

- Создан Go-модуль `github.com/MrArdek/UptimeControl`.
- Добавлена точка входа `main.go` с запуском и корректным завершением HTTP-сервера.
- Реализован публичный endpoint `GET /health`.
- Реализован endpoint готовности `GET /ready`, проверяющий PostgreSQL.
- Добавлены загрузка и проверка переменных `HTTP_ADDR` и `DATABASE_URL`.
- Добавлен `BASE_PATH`: приложение может работать под путём вроде `/uptimec` без переписывания URL на reverse proxy.
- Подключён пул соединений PostgreSQL через `pgx/v5`.
- Добавлена транзакционная система миграций с блокировкой параллельного запуска.
- Создана начальная схема пользователей, сессий, сайтов, проверок и инцидентов.
- Добавлены модульные тесты конфигурации, health и readiness endpoints.
- Реализованы регистрация, вход, выход и получение текущего пользователя.
- Пароли хешируются Argon2id; исходные токены сессий не сохраняются в БД.
- Добавлены проверка Origin, ограничения тела запроса и частоты регистрации/входа.
- Реализованы создание, список, просмотр, изменение и мягкое удаление сайтов.
- Все операции с отдельным сайтом проверяют владельца в SQL-запросе.
- Добавлена нормализация URL и блокировка явных локальных/private-адресов.
- Активный URL не может быть добавлен одному владельцу дважды.
- Реализованы универсальные проекты с HTTP-, TCP- и heartbeat-monitors.
- Старые сайты переносятся в проекты миграцией без потери проверок и инцидентов.
- Добавлен планировщик, безопасный HTTP checker, история и автоматические инциденты.
- Heartbeat позволяет мониторить Telegram-ботов, cron и workers без frontend.
- Первый пользователь становится владельцем установки, после чего регистрация закрывается.
- Добавлены Telegram-уведомления о падении и восстановлении.
- Добавлены исходящие webhooks: до 5 на проект, HMAC-подпись, retry и журнал доставок; Telegram продолжает работать рядом без изменений.
- Добавлен встроенный адаптивный Dashboard.
- Подготовлены Docker Compose и systemd-варианты установки на отдельный monitoring-сервер.
- Подготовлено описание продукта и предполагаемого технологического стека в `README.md`.
- Выбрана лицензия GNU GPLv3.
- Добавлен базовый `.gitignore` для Go-проекта.
- Зафиксированы контракты будущих Monitoring, Analytics и Metrics API: определения метрик, дедупликация, хранение, приватность, лимиты и нагрузочный профиль полного релиза.
- Добавлены cursor pagination проектов и истории, фильтры периода и сводки uptime/coverage/response time.
- Dashboard показывает график response time с разрывами данных, последний инцидент и его длительность.

## Что работает сейчас

- Приложение запускает HTTP-сервер на `:8080` или на адресе из `HTTP_ADDR`.
- При `BASE_PATH=/uptimec` служебные и API-маршруты доступны под `/uptimec`, а cookie ограничена этим путём.
- `GET /health` возвращает `200 OK` и JSON `{"status":"ok"}`.
- `GET /ready` возвращает `200 OK`, когда PostgreSQL доступен, или `503`, когда он недоступен.
- При запуске приложение подключается к базе и применяет ещё не выполненные миграции.
- API авторизации работает через серверную сессию в `HttpOnly` cookie.
- Авторизованный пользователь может управлять только собственными сайтами.
- Владелец может создавать проекты, добавлять HTTP/TCP/heartbeat monitors и смотреть историю за выбранный период.
- Планировщик выполняет проверки и обновляет состояние Dashboard.
- Падение или пропущенный heartbeat открывает инцидент, восстановление закрывает его.
- Открытие и закрытие инцидента рассылается через Telegram и подписанные webhooks; итог доставки webhook пишется в `webhook_deliveries`.
- При `SIGINT` или `SIGTERM` сервер получает до 10 секунд на корректное завершение запросов.
- Для PostgreSQL используется `github.com/jackc/pgx/v5` версии 5.10.0.
- Для Argon2id используется `golang.org/x/crypto` версии 0.56.0.

Модульные тесты, race detector, vet и сборка проверены с Go 1.27.1. Миграция TCP подготовлена, но в текущей среде нет PostgreSQL/Docker для интеграционного прогона.

## Что сейчас в работе

- Self-hosted MVP реализован и готов к установке на отдельный тестовый сервер.
- Реальная доставка Telegram будет проверена после настройки владельцем bot token и chat ID.
- Этап 9 полного плана завершён; следующий этап — устойчивая очередь доставки уведомлений.

## Что пока не готово

- Региональные узлы мониторинга.
- JavaScript-трекер и приём аналитических событий.
- Server Agent для системных метрик.
- Командные роли и приглашения.
- CI/CD и проверенная production-конфигурация TLS.

## Известные проблемы

- `api/v1/ui/analytics.go` содержит только пустую функцию с временным именем `asfdf`.
- В рабочей папке находится неотслеживаемый временный файл `.DS_Store.swp`.
- Реализованные миграции пока запускаются самим приложением; для нескольких production-реплик позднее может потребоваться отдельная команда миграции.
- Для production ещё не настроены TLS, сервисный менеджер, резервные копии и CI/CD.
- Ограничение частоты запросов хранится в памяти одного процесса; для нескольких реплик потребуется общий limiter или ограничение на reverse proxy.
- Telegram-интеграция не может быть проверена на реальной доставке без токена бота и chat ID.
- Docker недоступен в текущей локальной среде, поэтому образ и Compose-конфигурация проверены вручную, но не собраны локально.

## Важные технические детали

- Планируемая основа: Go, TypeScript и PostgreSQL.
- Основная модель поставки — self-hosted на сервере пользователя.
- HTTP-сервер пока использует только стандартную библиотеку Go.
- Адрес сервера задаётся через `HTTP_ADDR`; значение по умолчанию — `:8080`.
- `BASE_PATH` задаёт URL-префикс приложения; пустое значение оставляет маршруты в корне.
- `PUBLIC_ORIGIN` содержит только схему и домен, без `BASE_PATH`.
- `DATABASE_URL` обязателен и не должен попадать в логи.
- Идентификаторы основных сущностей имеют тип UUID и будут создаваться приложением.
- Временные ряды на первом этапе хранятся в PostgreSQL.
- Python рассматривается только как возможный будущий сервис расширенной аналитики.
- Метрики сервера невозможно получить браузерным трекером: для них нужен отдельный агент.

## Что важно помнить

- README сейчас является описанием желаемого продукта, а не перечнем реализованных возможностей.
- Проект — универсальный контейнер для сайта, API, игры, бота или другого сервиса; конкретные проверки будут отдельными monitors.
- Для MVP приняты email/пароль, серверная cookie-сессия и один владелец установки.
- `SESSION_COOKIE_SECURE` должен оставаться `true` на HTTPS-сервере; `false` разрешён только для локального HTTP.
- Удаление сайта мягкое: запись и история сохраняются, сайт отключается.
- Изменения схемы базы данных после её появления должны выполняться миграциями.
- Секреты нельзя добавлять в репозиторий; они должны храниться в локальном `.env`.
